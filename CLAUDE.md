# Lemma — CLAUDE.md

## Проект

PWA-приложение для изучения английских слов (бывш. WordPunk).
React + TypeScript + Vite + IndexedDB (idb). Без бэкенда — всё локально.

**Цель проекта: веб-приложение, доступное по ссылке (GitHub Pages). БЕЗ публикации в App Store / Google Play.** Нативные сторы и Capacitor-обёртка — архивированы (см. Stage E).

GitHub: https://github.com/emorozoff/lemma
Deploy: https://emorozoff.github.io/lemma/

## Версия

Текущая версия: **v1.231**

Версия отображается в интерфейсе (хедер, рядом с логотипом).

**ОБЯЗАТЕЛЬНО:** при КАЖДОМ коммите, даже мельчайшем фиксе, инкрементировать версию.
Пользователь смотрит версию в хедере чтобы понять, обновилась ли страница.
Без бампа версии пользователь не видит что обновление доставлено.

Правила инкремента:
- Мелкие правки: третья цифра (v0.84 → v0.841 → v0.842)
- Значимые изменения: вторая цифра (v0.84 → v0.85)
- Крупные обновления: первая цифра — только по команде пользователя

Версию обновлять в ДВУХ местах:
1. `src/components/MainScreen.tsx` — span.header-version
2. `CLAUDE.md` — поле «Текущая версия» выше

## Git / Deploy

После каждого изменения: `git add . && git commit -m "..." && git push`
Не спрашивать подтверждения — просто пушить.
GitHub Actions автоматически деплоит в GitHub Pages при пуше в main.

## Структура

```
src/
  App.tsx              — роутинг между экранами
  components/
    MainScreen.tsx     — главный экран с карточками
    AddWordModal.tsx   — модалка добавления слова
    TopicModal.tsx     — выбор темы
    StatsScreen.tsx    — статистика
    LevelUpPopup.tsx   — попап нового уровня
  data/
    words.ts           — база слов
    topics.ts          — темы
  db/index.ts          — IndexedDB (idb)
  lib/
    srs.ts             — алгоритм SRS + система уровней
    audio.ts           — звуки
  styles/global.css    — все стили
  types/index.ts       — TypeScript типы
```

## Стиль (с v0.9 — минимализм, без киберпанка)

- Шрифт: IBM Plex Mono везде (`--font-ui`/`--font-display`/`--font-logo`), из Google Fonts (index.html). (`--font-logo` оставлен отдельной переменной на случай, если вордмарку захочется отдельный шрифт.)
- Палитра: чёрный/белый + **один** акцент `--accent: #e5342b` (красный). Токены в `:root`, тинты через `color-mix(in srgb, var(--accent) N%, transparent)` (`--accent-soft`, `--accent-line` и т.д.).
- Семантика цвета: **красный = активно/верно/бренд**, **серый (`--text-muted`) = неактивно/неверно**. Легаси-алиасы: `--accent-green`/`--accent-amber` → `--accent`; `--accent-red` → `--text-muted`.
- Светлая/тёмная тема: атрибут `[data-theme]` на `<html>`. **Главная тема — СВЕТЛАЯ (по умолчанию).** Тёмная (`[data-theme="dark"]`) включается ТОЛЬКО вручную в Настройках. Режим «авто/system» убран (v1.11); legacy-значение `lemma_theme='system'` трактуется как светлая. `ThemeProvider` + `lib/theme.ts` (ThemeMode = 'light'|'dark', `THEME_ORDER=['light','dark']`), переключатель в Настройках, ключ localStorage `lemma_theme`, pre-paint скрипт в `index.html` (без мигания; дефолт light).
- Тёмная: фон `#0a0a0a`; светлая: фон `#fff`. Без неона и глитч-хроматики. Едва заметная горизонтальная скан-текстура — в фоне `body` (слоями: gradient над `--bg`), цвет `--scanline-color`, темо-зависимая, **под всем контентом** (кнопки/карточки на surface-фоне перекрывают её, текст выше).
- Вордмарк: `lemma_` (строчные + подчёркивание). Кнопка флага — только красная иконка `⚑` (opacity 0.45 → 1 при отметке).

## Планы на v1.0+

### Страница «Настройки» — реализовано в v0.82
Открывается кнопкой НАСТРОЙКИ в нижней навигации.
Содержимое сейчас:
- Озвучка слова (вкл/выкл)
- Темы (открывает TopicModal)
- Мои слова (открывает AddWordModal)
- Сбросить прогресс (с подтверждением)

Текущие пункты также:
- **нестрогий ввод** (вкл/выкл) — реализовано в v1.2. По умолчанию ВЫКЛ = строгая проверка ручного ввода (точное совпадение). ВКЛ = прощается ошибка в одну букву (`checkManualAnswer(..., lenient)` в srs.ts, ключ `lenient_input`). Это **глобальный тумблер: делает мягкими ВСЕ вводы в игре, включая финал** (v1.21; быстрый ввод, финал lvl 4, архив-челлендж).

Планируется добавить:
- Режим озвучки (слово / предложение / нет) — когда появится озвучка предложений
- Загрузить все аудио — реализовано в v0.96 (кнопка с прогресс-баром, фетчит все слаги → SW CacheFirst)

Убрано (v1.2): настройка «ввод слов на финале» — теперь финал ВСЕГДА ручной ввод (это и финализирует слово в запоминании). `isManualInputEnabled` удалён, `isFinale = isFastInputEnabled() || currentLevel === MAX_LEVEL`.

### Режим быстрого ввода — реализовано в v0.96 (тумблер в Настройках)
Переиспользует механизм финала: `isFinale` включает `isFastInputEnabled()`, ответ через `handleManualSubmit` → правильно = `processFinaleAnswer` (archived), ошибка = мягкий сброс на завтра. В режиме берётся 60 новых карточек за сессию вместо 20. Когда ВКЛючён:
- Каждая карточка показывает русское слово; пользователь сам **вписывает перевод на английском** (переиспользует UI ручного ввода с буквенными ячейками, как на финале lvl 4, но для ВСЕХ карточек).
- Правильно вписал → слово **сразу засчитывается как выученное** (минуя пошаговый SRS): +1 в «знаю слов» немедленно.
- Зачем: на старте все слова слишком простые — приходится каждый раз зажимать вариант и вводить слово, хотя ошибиться невозможно. Режим даёт быстро «пролистать» тривиальные слова.
- Пользователь **сам выключает** режим, когда дойдёт до сложных слов (возвращается обычный SRS с выбором из 4).

### Функциональные хвосты до v1.0 (Фаза 2)
- Пересчёт шкалы уровней игрока под реальную базу (потолок srs.ts всё ещё 3000/B1 при ~7900 карточек)
- Бэкап / экспорт-импорт прогресса — реализовано в v0.97 (сохранить/восстановить прогресс в Настройках: JSON-файл с progress/activity/flagged + localStorage; импорт перезаписывает и перезагружает)
- Кастомный домен для R2-аудио — ОТЛОЖЕНО (пользователь: max 5 юзеров в обозримом будущем, dev-домен `*.r2.dev` для такого объёма ок). Вернуться при масштабировании / появлении своего домена: настроить custom domain в Cloudflare → сменить `AUDIO_CDN` в src/lib/audio.ts

### Обучение / онбординг для новых пользователей — ЗАПЛАНИРОВАНО (реализовать по команде)
Гайд-тур (spotlight/coachmark) при ПЕРВОМ запуске. Механика (по ТЗ пользователя):
- Поверх интерфейса — затемнение; **подсвечен один элемент** + подпись-инструкция рядом («тут настройки, можно выбрать темы» и т.п.).
- Реальный UI на время тура **неинтерактивен** (ничего нельзя реально нажать).
- Тап по **любой части экрана** → следующий шаг: подсвечивается следующий элемент + следующая подпись.
- Показывать **только новым** (флаг в localStorage, напр. `lemma_onboarded`; запускать когда флага нет). На последнем шаге — выставить флаг. Сброс прогресса/импорт бэкапа — продумать, сбрасывать ли флаг.
- Реализация: оверлей с «дыркой» (cutout) вокруг target по его bounding-rect; элементы уже имеют классы для таргетинга (`header-known`, `level-bar`, `word-card`, `options-grid`, `flag-btn`, `nav-btn` НАСТРОЙКИ/СТАТИСТИКА).

Чему учим (КОНТЕНТ ШАГОВ — вариант выбирает пользователь). Два must-have: (1) **как отвечать** — тап по правильному переводу выделенного слова (зажатие варианта = шорткат на ввод/архив, для новичка можно опустить); (2) **выбрать темы в Настройках** — без тем экран пустой («НЕТ СЛОВ_ … либо не выбрал темы»). Предложенные варианты:
- **A — Минимум (3 шага):** ответ (тап перевода) → Настройки (выбери темы, без них слов нет) → «знаю слов».
- **B — Стандарт (5–6, рекомендуется):** A + шкала уровня + флажок ⚑ + тап по карточке = озвучка.
- **C — Полный (~8):** B + Статистика + быстрый ввод + тема. (Риск: длинный тур пролистывают не читая.)

### Stage D — Аудио (следующий этап)
- Миграция аудио на CDN (сейчас ~83МБ в /audio/ на GitHub Pages)
- Генерация озвучки предложений (~200МБ, Edge TTS)
- Тап по предложению → воспроизведение полного предложения
- Тап по слову в мини-истории → озвучка этого слова
- Настройка режима озвучки (слово / предложение / нет) в настройках

### Stage E — Публикация в сторы — АРХИВИРОВАНО
Решение пользователя: цель — веб-приложение по ссылке (GitHub Pages), нативных сторов нет. НЕ делаем: аккаунты разработчика (Google Play $25 / Apple $99), публикацию в App Store / Google Play, 12 тестировщиков. Capacitor-каркас в `ios/` остаётся в репо как есть, но не развивается. Виброотдача — уже есть в `lib/native.ts` (no-op в браузере). Бэкап — реализован в v0.97.

## UX-принципы

- **Зона большого пальца**: все интерактивные элементы (кнопки ответов, кнопка ДАЛЕЕ) должны быть внизу экрана, в зоне досягаемости большого пальца на телефоне. Карточка со словом — сверху/в центре, кнопки — снизу.

- **Sentence-first обучение (навсегда)**: все карточки показывают предложение как главный элемент, изучаемое слово выделено внутри предложения. Пользователь учит слова в контексте, не в вакууме. Для многозначных слов (lose = потерять/проиграть) делаем отдельные карточки с разными примерами — каждая раскрывает одно конкретное значение. Единый UX для всех карточек, без исключений.

## База слов

- **Целевой размер базы: 7000 слов** (с запасом — пользователь планирует вручную удалить много слов позже, поэтому нужен буфер).
- Одно английское слово может быть в нескольких карточках, если имеет разные значения (каждое значение — отдельная карточка с примером, раскрывающим именно это значение).
- Счётчик уникальных слов в шапке считается по английскому слову, а не по количеству карточек.

### Удаление карточек — ВАЖНО

При ревизии и удалении карточек **редактировать `src/data/words.ts` НАПРЯМУЮ**, а НЕ удалять строки из `data/raw/*.csv` с пересборкой.

Причина: `scripts/build-words.mjs` генерирует ID последовательно (`airport_001`, `airport_002`...). Если удалить строку из CSV и пересобрать, все последующие ID сдвинутся → у пользователей в IndexedDB прогресс «съедет» на соседние карточки (например, прогресс по `airport_044 layover` начнёт ссылаться на `airport_044 transit`). Прямое редактирование `words.ts` сохраняет стабильные ID.

Побочный эффект: `data/raw/*.csv` расходятся с `words.ts`. Это терпимо — `words.ts` де-факто становится source of truth. Если в будущем понадобится снова собирать из CSV, сначала надо рефакторить `build-words.mjs` на стабильные ID (например, хеш от `topicId+english+russian`).

## SRS v2 (с v0.83)

- **Уровни:** 0–4. Интервалы: `[0, 1, 3, 7, 14]` дней.
- **Уровень 0:** знакомство, 2 правильных подряд → lvl 1. Ошибка сбрасывает серию.
- **Уровни 1–4:** выбор из 4. Правильно → +1 уровень (для `difficulty` 1–2: +2). Ошибка → −1 (минимум 1).
- **Уровень 4 = финал.** ВСЕГДА ручной ввод (настройка убрана в v1.2). Строгость — по общей настройке «нестрогий ввод» (по умолчанию строго; точный ввод финализирует слово → архив).
- **Счётчик «знаю слов»:** level × 0.2 (1→0.2, 2→0.4, 3→0.6, 4→0.8), архив = 1.0, сумма округляется.
- Единый источник — `getKnownCount()` в `src/db/index.ts`; оптимистичные +1 в UI удалены.

## Мелкие баги

Фиксить сразу без согласования.
